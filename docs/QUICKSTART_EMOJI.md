# Быстрый старт: Эмодзи для подписок

## Запуск в 3 шага

### 1. Обновите базу данных

```bash
psql -h <host> -U <user> -d <database> -f migrations/add_emoji_field.sql
```

Или вручную:
```sql
ALTER TABLE subscriptions ADD COLUMN IF NOT EXISTS emoji VARCHAR(10) DEFAULT '▫️';
UPDATE subscriptions SET emoji = '▫️' WHERE emoji IS NULL;
```

### 2. Перезапустите бота

**Docker:**
```bash
docker compose restart
```

**PM2:**
```bash
pm2 restart recur_bot
```

**Node.js:**
```bash
# Остановите бота (Ctrl+C) и запустите снова
node node.js
```

### 3. Готово!

Теперь вы можете:
- Добавлять новые подписки с эмодзи через `/add`
- Редактировать эмодзи существующих подписок через `/edit` → Эмодзи
- Видеть эмодзи во всех командах: `/list`, `/month`, `/upcoming`

## Примеры использования

### Создание подписки с эмодзи

1. `/add`
2. Введите название: `Netflix`
3. **Выберите эмодзи** 🎬 из списка или нажмите "Свой эмодзи"
4. Продолжите заполнение...

### Изменение эмодзи

1. `/edit`
2. Выберите подписку
3. Нажмите **Эмодзи**
4. Выберите новый эмодзи

### Сброс на дефолтный

1. `/edit`
2. Выберите подписку
3. Нажмите **Эмодзи**
4. Нажмите **Установить дефолтный ▫️**

## Что если не хочу эмодзи?

Просто нажмите "Пропустить" при создании подписки. Будет использован дефолтный эмодзи ▫️

## Проблемы?

- **Эмодзи не отображаются?** Убедитесь, что выполнили миграцию БД
- **Бот не запускается?** Проверьте логи: `docker logs recur_bot` или `pm2 logs`
- **Ошибки при добавлении?** Попробуйте перезапустить бота
