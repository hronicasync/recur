# Быстрая диагностика recur_bot

## Проблема: Команда /checknotifications не появилась в меню

### Что делать прямо сейчас

#### Шаг 1: Проверьте что команда работает
Просто введите в боте вручную:
```
/checknotifications
```

**Если команда отвечает** ✅ - всё работает! Проблема только в кэше Telegram. Переходите к Шагу 3.

**Если команда не отвечает** ❌ - переходите к Шагу 2.

#### Шаг 2: Проверьте статус бота на сервере

**Вариант A - Автоматическая проверка:**
```bash
ssh yourserver
cd ~/recur
./checkBotStatus.sh
```

**Вариант B - Ручная проверка:**
```bash
ssh yourserver

# Проверить что контейнер запущен
docker ps | grep recur_bot

# Проверить логи
docker logs recur_bot --tail 50

# Проверить что команды зарегистрированы
docker logs recur_bot --tail 50 | grep "Bot commands registered"

# Проверить планировщик
docker logs recur_bot --tail 100 | grep "Reminder scheduler started"
```

**Если бота нет в логах или есть ошибки:**
```bash
cd ~/recur
git pull
docker compose down
docker compose up -d --build
docker logs -f recur_bot
```

#### Шаг 3: Очистите кэш Telegram (если команда работает, но не в меню)

**Мобильный (Android/iOS):**
1. Настройки → Данные и память
2. Использование памяти → Очистить кэш
3. Перезапустить приложение
4. Открыть бота и проверить меню команд (/)

**Desktop:**
1. Выйти из аккаунта
2. Войти снова
3. Открыть бота и проверить меню

**Web:**
1. Открыть в режиме инкогнито
2. Или очистить кэш браузера

**Альтернатива - через @BotFather:**
1. Открыть [@BotFather](https://t.me/BotFather)
2. Отправить `/mybots`
3. Выбрать вашего бота
4. Edit Bot → Edit Commands
5. Вставить список (см. ниже) и отправить

```
start - онбординг и список команд
add - добавить новую подписку
list - все подписки и суммы
upcoming - списания на 7 дней вперёд
month - оплаченные и ожидающие в текущем месяце
edit - редактировать подписку
pay - отметить оплату вручную
delete - удалить подписку
reminders - управлять напоминаниями
dbstatus - проверить подключение к базе
checknotifications - проверить статус напоминаний
```

## Полная диагностика

### Проверка 1: Контейнер работает
```bash
docker ps | grep recur_bot
```
Должен показать запущенный контейнер.

### Проверка 2: Бот запустился
```bash
docker logs recur_bot --tail 50 | grep "Запустился как"
```
Должно быть: `Запустился как @your_bot_name`

### Проверка 3: Команды зарегистрированы
```bash
docker logs recur_bot --tail 50 | grep "Bot commands registered"
```
Должно быть: `✅ Bot commands registered: start, add, list, upcoming, month, edit, pay, delete, reminders, dbstatus, checknotifications`

### Проверка 4: Планировщик работает
```bash
docker logs recur_bot -f | grep "🔄 Tick"
```
Каждую минуту должен появляться новый тик: `🔄 Tick #1`, `🔄 Tick #2`, и т.д.

### Проверка 5: Нет ошибок
```bash
docker logs recur_bot --tail 200 | grep -i "error\|❌"
```
Не должно быть критических ошибок.

## Ожидаемый ответ команды /checknotifications

```
📡 Статус планировщика:
✅ Запущен
Последний тик: 12:34:56
Всего тиков: 42

⚙️ Ваши настройки:
Часовой пояс: Europe/Moscow
Время напоминаний: 10:00
Предварительные: T-3, T-1, T0

📋 Активные подписки: 5
...список подписок...

📜 История (последние 5):
...история напоминаний...
```

## Если ничего не помогает

1. **Полный перезапуск с rebuild:**
   ```bash
   cd ~/recur
   git pull
   docker compose down
   docker compose up -d --build
   docker logs -f recur_bot
   ```

2. **Подождите 30 минут** - иногда Telegram кэш обновляется с задержкой

3. **Проверьте в другом Telegram клиенте** (например, если не работает на телефоне - попробуйте web версию)

4. **Свяжитесь с автором:** [@hanumatori](https://t.me/hanumatori)

## Дополнительные ресурсы

- **Полная документация:** [docs/PROJECT_OVERVIEW.md](docs/PROJECT_OVERVIEW.md)
- **Деплой:** [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md)
- **История изменений:** [CHANGELOG.md](CHANGELOG.md) (локально, не в git)
